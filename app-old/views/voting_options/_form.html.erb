<%= form_with(model: voting_option) do |form| %>
  <% if voting_option.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(voting_option.errors.count, "error") %> prohibited this voting_option from being saved:</h2>

      <ul>
        <% voting_option.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
