<%= form_with(model: dietary_restriction) do |form| %>
  <% if dietary_restriction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dietary_restriction.errors.count, "error") %> prohibited this dietary_restriction from being saved:</h2>

      <ul>
        <% dietary_restriction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
