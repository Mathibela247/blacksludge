<%= form_with(model: type_of_share) do |form| %>
  <% if type_of_share.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(type_of_share.errors.count, "error") %> prohibited this type_of_share from being saved:</h2>

      <ul>
        <% type_of_share.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
