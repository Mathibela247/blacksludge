<%= form_with(model: proxy_type) do |form| %>
  <% if proxy_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(proxy_type.errors.count, "error") %> prohibited this proxy_type from being saved:</h2>

      <ul>
        <% proxy_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
